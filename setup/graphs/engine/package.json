{
  "name": "aigang-graph-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Graph-based process engine (schema validation, addressing, walking) and parallel branch coordination for AI Gang V2 initialization/setup procedures.",
  "main": "lib/index.js",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
