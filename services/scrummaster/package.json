{
  "name": "scrummaster",
  "version": "1.0.0",
  "description": "AI Gang ScrumMaster — deterministic routing service between Jira and agent containers",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "pretest": "node scripts/check-test-redis.js",
    "test": "node --test --test-concurrency=1"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.0.0",
    "express": "^4.19.0",
    "redis": "^4.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
